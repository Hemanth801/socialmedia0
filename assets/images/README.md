Place your professional headshot here as `profile.jpg`.

To add the photo you attached in the chat:
1. Save your image as `profile.jpg` (recommended size 400x400, square). 
2. Put the file at: `assets/images/profile.jpg` in the project folder.
3. The portfolio already references `assets/images/profile.jpg` and will display it automatically.

Notes:
- Use a professional headshot (plain background, business attire).
- If you prefer PNG, rename to `profile.jpg` or update `index.html` accordingly.
